---
title: Site
---

## Conf

### base

The `base` [configuration](conf.md) mount the website URL path to its value.

For instance, if the base is set to `/mybase`. The expected website root would be `https://www.example.com/mybase`

By default, the value is `/`

