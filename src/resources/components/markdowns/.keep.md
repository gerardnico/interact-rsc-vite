Markdown registration is done with code at configuration
Not with the directory registration.

Why?
Because we want our components to be available at export via `@combostrap/interact/components/*`

This file keep the directory alive so that the registration does not fail.